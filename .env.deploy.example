# Open-Meeting Deploy Configuration
# Copy this file to .env.deploy and fill in your server details.
# .env.deploy is git-ignored and will NOT be committed.

# Your Hetzner server IP or hostname
DEPLOY_HOST=open-meeting.com

# SSH user (default: root)
DEPLOY_USER=root

# Path where the repo is cloned on the server (default: /opt/open-meeting)
DEPLOY_PATH=/opt/open-meeting

# SSH key path (optional — leave empty to use default SSH key)
SSH_KEY=C:\Users\GNO\.ssh\nobel-net
